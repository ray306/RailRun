"""
生成目标知识 (production-finalize)、反馈与螺旋式迭代。
由于反馈在结果之后，第一次迭代可以从“最小可行产品 (Minimum Viable Product)”出发，先完成一个“还不错”的版本，然后基于反馈进行迭代，而不是要求一次性完美。
"""

# 螺旋式迭代
while True:
    step:
        根据制定的行动策略生成目标知识
        
    """
    在执行中，你需要不断对照“任务简报”，分析是否功能有效（如 “这一页PPT对听众来说信息量是不是太大了？）” 和负责（如 “这个简化是否会误导理解？”）
    """
        
    step:
        压力测试(defender="自己", debate_mode=True)

    step:
        输出情况总结并寻求反馈
    
    暂停点()

    if 通过:
        break