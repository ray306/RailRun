"""
差距分析(gap-analysis)。基于之前分析的信息，确定任务与现况（从“源知识”到“目标知识”）之间的鸿沟。
"""

step:
  分析**内容差距**：
  根据任务简报定义的目标知识，源知识是否缺少、缺少了哪些重要的信息？如果是，缺少了哪些？

step:
  分析**结构差距**：
  源知识当前的组织方式是否符合任务简报定义的目标知识的要求？如果是，哪些有差距？

step:
  分析源知识和目标知识之间的**其它重要差距**

step:
  压力测试(defender="自己", debate_mode=False)

step:
  系统必须在此处输出一份简明指令，形成指示后续流程重点关注的探索导向
  然后输出 "确定探索导向🚩，后续流程将重点关注以上盲点"